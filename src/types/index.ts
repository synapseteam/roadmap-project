export type RoutesType = {
  home: string;
  featureRequests: string;
};

export type CardType = {
  title: string;
  id: number;
  likes: number;
  comments: number;
};

export type SideBarForm = {
  title: string;
  description?: string;
};
